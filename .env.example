# Rename this file to .env.local before running locally,
# and add the same variable in Vercel/Render's dashboard when deploying.

# Get a key from https://platform.openai.com/api-keys (or swap the provider in lib/llm.js)
OPENAI_API_KEY=your_openai_api_key_here
